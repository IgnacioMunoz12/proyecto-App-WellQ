<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_wellq">

    <!-- ===== Permisos ===== -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Health Connect (lectura/escritura). Puedes podarlos luego si quieres. -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_NUTRITION" />
    <uses-permission android:name="android.permission.health.READ_HYDRATION" />
    <uses-permission android:name="android.permission.health.WRITE_STEPS" />
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_WEIGHT" />
    <uses-permission android:name="android.permission.health.WRITE_SLEEP" />
    <uses-permission android:name="android.permission.health.WRITE_NUTRITION" />
    <uses-permission android:name="android.permission.health.WRITE_HYDRATION" />

    <uses-permission android:name="androidx.health.connect.permission.READ_HEART_RATE" />
    <uses-permission android:name="androidx.health.connect.permission.WRITE_HEART_RATE" />
    <uses-permission android:name="androidx.health.connect.permission.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="androidx.health.connect.permission.WRITE_RESTING_HEART_RATE" />
    <uses-permission android:name="androidx.health.connect.permission.READ_STEPS" />
    <uses-permission android:name="androidx.health.connect.permission.WRITE_STEPS" />
    <uses-permission android:name="androidx.health.connect.permission.READ_WEIGHT" />
    <uses-permission android:name="androidx.health.connect.permission.READ_SLEEP" />
    <uses-permission android:name="androidx.health.connect.permission.READ_WATER" />
    <uses-permission android:name="androidx.health.connect.permission.WRITE_WATER" />
    <uses-permission android:name="androidx.health.connect.permission.READ_NUTRITION" />

    <!-- ===== QUERIES (hermano de application) ===== -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>

    <!-- ===== APPLICATION (hermano de queries) ===== -->
    <application
        android:label="WellQ"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <!-- Nota: eliminé android:name para evitar el warning de ${applicationName}.
             Si lo quieres, puedes añadir: android:name="${applicationName}" -->

        <activity
            android:name="com.example.app_wellq.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <receiver
            android:name=".NotificationPublisher"
            android:exported="false" />
        <!-- Importante: SIN receivers de flutter_local_notifications,
             porque volviste a tu NotificationService simple. -->
    </application>

</manifest>
